import React from "react";
import { FooterContainer, GlobalContainer } from "../styles/GlobalStyle.styled";

const Footer = () => {
  return (
    <FooterContainer>
      <GlobalContainer>Footer</GlobalContainer>
    </FooterContainer>
  );
};

export default Footer;
